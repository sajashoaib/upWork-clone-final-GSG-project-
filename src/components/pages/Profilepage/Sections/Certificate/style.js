import { Button, styled } from "@mui/material";

export const StyledDivcertificate = styled("div")(({ theme }) => ({
  display: "flex",
  gap: "15px",
}));
export const Styledbtncertificate = styled(Button)(({ theme }) => ({
  textTransform: "none",
  color: "green",
}));
